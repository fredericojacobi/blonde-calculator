import * as React from 'react';
import { HStack, Center, Button, View } from 'native-base';
import { useState } from 'react';

export default function Solidos(){
  
  return (
    <View>
      <Button bg='blue.400'>Click Me</Button>
    </View>
  );
}